# ============================================================
# NexoFin - Agente Inteligente RAG
# ============================================================
# Instalación: pip install -r requirements.txt

# ─── Web Framework ──────────────────────────────────────────
streamlit>=1.38.0

# ─── LangChain Core ─────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0
langchain-text-splitters>=0.3.0

# ─── Cohere (LLM + Embeddings) ─────────────────────────────
langchain-cohere>=0.4.0
cohere>=5.0.0

# ─── OpenRouter (LLM fallback) ─────────────────────────────
langchain-openrouter>=0.2.0

# ─── HuggingFace (Embeddings fallback, usado con OpenRouter) ─
# langchain-huggingface>=0.1.0
# sentence-transformers>=3.0.0
# torch>=2.0.0  # Requerido por sentence-transformers (~2GB)
#
# NOTA: Solo necesario si usás LLM_PROVIDER=openrouter
# Descomentá las líneas de arriba si querés ese modo.

# ─── PDF Processing ─────────────────────────────────────────
pypdf>=5.0.0

# ─── PDF Generation (solo para crear el documento fuente) ──
fpdf2>=2.7.0

# ─── Environment ────────────────────────────────────────────
python-dotenv>=1.0.0
